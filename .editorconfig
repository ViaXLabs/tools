root = true

[*]
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

# Apply rules only to YAML/YML files inside a .harness directory
[.harness/**/*.{yaml,yml}]
indent_style = space
indent_size = 2
match_pattern = ^-\s*
indent_size = 4
