Two important findings before I build the toggles:

1. **New Relic has a real, distinct GovCloud endpoint for exactly this integration**: `gov-aws-api.newrelic.com` for AWS Metric Streams ingest (confirmed from New Relic's own FedRAMP docs) — separate from the commercial `aws-api.newrelic.com` we've been using. Your "gov dash AWS website" instinct was right; it's a real, documented thing.

2. **But there's a bigger catch**: New Relic's own GitHub issue tracker confirms cloud integrations are explicitly out of scope for FedRAMP, and the New Relic Terraform provider's cloud-integration resources have no option to point at .gov ingest endpoints. That means if your New Relic account is FedRAMP-designated, the account-link/integrations piece in `newrelic.tf` (the `newrelic_cloud_aws_link_account`/`newrelic_cloud_aws_integrations` resources) may not be usable the same way at all — only the metric-stream ingest URL has a `.gov` variant. Worth confirming with your New Relic account rep which of your NR account's features are actually FedRAMP-scoped before we assume the whole stack needs to move to `.gov` endpoints.

3. **One thing your "with/without S3" ask runs into**: I confirmed `s3_configuration` is a *mandatory* nested block for `http_endpoint_configuration` — AWS's API requires it, not just our Terraform. There's no "no S3 bucket" variant possible for this destination type. The useful equivalent is `s3_backup_mode`: `FailedDataOnly` (what we have) vs `AllData` — and `AllData` is actually a great debugging tool right now, since it would show you *every* record Firehose receives regardless of whether HTTP delivery to New Relic succeeds. If you switch to `AllData` and still see nothing in S3, that tells you records aren't even reaching Firehose — a completely different problem than KMS.

Let me build this as toggles so you can flip through combinations without hand-editing files each time:Now update `locals.tf` for the govcloud endpoint variant:Now update `iam.tf` — the KMS-access policy needs to be conditional too, and reference `aws_kms_key.firehose[0]`:Now `s3.tf` — the backup bucket's encryption should follow the same toggle:Now the main `firehose.tf` — conditional SSE block, the `s3_backup_mode` toggle, and fixing the `depends_on` list for the now-conditional resources:Now `outputs.tf` needs fixing since it references the KMS key directly:Now let's grep across every file for any remaining unguarded references to the KMS key resource that I might have missed:All references properly guarded. Let's re-run the balance check across everything and refresh the delivered files:All balanced. Let's also update the tfvars example to document the new toggles, then push everything:**Also — on "no data anywhere, not even S3":** that's actually a useful confirming detail, not a contradiction. If KMS is the real blocker (access denied, disabled, or throttled), Firehose can't produce an encrypted buffer *at all* — and that buffer feeds *both* the New Relic HTTP delivery **and** the S3 backup path. A KMS failure upstream of both destinations would explain seeing nothing in either place simultaneously, which is more consistent with our KMS hypothesis than it is a separate mystery. Setting `s3_backup_mode = "AllData"` now is a good way to confirm this directly: if data still never shows up in S3 even with `AllData`, that's strong evidence the failure is happening before Firehose can write *anywhere* — which really only fits a KMS or ingestion problem, not a New Relic-endpoint-specific one.

**What I changed, in three toggles (all in `variables.tf`, default is your current hardened config):**

- **`enable_customer_managed_kms`** (default `true`) — set to `false` to reproduce your original working no-KMS baseline exactly, with everything else (IAM, CloudWatch logging, metric stream) identical. This makes KMS a true isolated variable instead of an all-or-nothing rebuild.
- **`s3_backup_mode`** (default `"FailedDataOnly"`) — set to `"AllData"` to see every record Firehose receives, independent of delivery success. Use this first — it's the fastest way to check "is Firehose even getting data" before worrying about KMS or New Relic at all.
- **`newrelic_endpoint_variant`** (default `"commercial"`) — set to `"govcloud"` to point at New Relic's documented `gov-aws-api.newrelic.com`. Worth testing only after confirming with your NR rep that your account is actually FedRAMP-designated for this feature, given the out-of-scope caveat above.

To run a specific combination, just override in `terraform.tfvars` or on the CLI, e.g.:

```bash
terraform apply -var="enable_customer_managed_kms=false" -var="s3_backup_mode=AllData"
```

I'd suggest starting with `s3_backup_mode=AllData` + your current KMS setting, before touching KMS at all — it isolates "is data reaching Firehose" as its own question first.
